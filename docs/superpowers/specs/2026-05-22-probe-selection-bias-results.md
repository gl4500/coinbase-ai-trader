# Probe Selection-Bias Meta-Analysis

Spec: `2026-05-22-probe-selection-bias-design.md`. Deflates the recorded
XGB feature-search results for the size of the search and asks whether the
project's apparent edge survives once you account for how much was tried.

> **Doc note.** The 17-row Trial table and the 18-row Deflation table below
> are auto-generated by `python -m tools.probe_selection_bias`. The narrative
> sections (AUC definition, the two noise scales, Findings, Roadmap) were
> hand-added on top of the generated output so the doc reads standalone for
> someone arriving cold.

## What AUC is, and what we're measuring

**AUC = Area Under the Receiver Operating Characteristic Curve.** A single
number summarizing how well a binary classifier *ranks* positive examples
above negatives.

| Value | Meaning |
|---|---|
| 1.0 | Perfect — every positive scores higher than every negative |
| 0.5 | Random — coin-flip ranking; the model knows nothing |
| 0.0 | Perfectly *backwards* — fix by inverting the score |

**Intuitive reading.** AUC is the probability that a randomly picked positive
sample gets a higher model score than a randomly picked negative sample. So
AUC 0.512 = 51.2% of the time; AUC 0.55 (the project's production gate) =
55%; AUC 0.60+ = strong by financial-ML standards; AUC 0.65+ = very strong,
rare in noisy markets.

The probes in this project predict whether a triple-barrier UP outcome
fires (or, for v3, `close[t+k] > close[t]`). Every number in the deflation
table below is being compared against the null hypothesis **AUC = 0.5** —
a model that knows nothing.

The diagnostic does **not** measure profitability, calibration, or fee
impact — only ranking quality. (That's why the deployment scorecard exists
separately — see Session 58.71l: v3 fails 3 of 4 deployment gates even with
its small AUC edge.)

## Verdict

VERDICT: marginal — deflated probability 0.74 is above the noise floor but
short of the 0.95 bar; the edge is weak and selection-fragile.

## The two noise scales

Every deflation cell below is anchored on one of two standard errors (SE) of
the AUC under the null:

| Noise model | SE | What it assumes |
|---|---|---|
| **iid** | 0.00141 | Every one of the ~167k cache samples is an independent draw. Textbook AUC variance. |
| **fold** (honest) | 0.00899 | The empirical spread of v3's AUC across the 5 purged-WF folds: `[0.516, 0.507, 0.527, 0.523, 0.529]`. |

**The fold SE is 6.4× larger than the iid SE** because triple-barrier labels
at a 4h horizon on hourly bars overlap ~75% with their neighbours — the
*effective* sample size is far below 167k. The fold SE measures what the
AUC actually does on genuinely time-separated data; iid is the textbook
mistake Bailey & López de Prado (AFML) call out.

Every iid row in the table below pegs at 1.000. Read those as a contrast
exhibit — "what naive significance testing would tell you, which is wrong
for overlapping financial labels" — not as the verdict.

## Trial table

17 single-add channel-candidate probes (marketcap excluded —
null-coverage). Source: `xgb_probe_results_log.md`.

| probe | Delta AUC | passed |
|---|---|---|
| RSI-rank (survivorship) | +0.0124 | True |
| RSI-rank (legacy) | +0.0208 | True |
| log10-vol-rank | -0.0010 | False |
| MFI-rank | +0.0031 | False |
| BTC-dominance | +0.0077 | False |
| OKX long/short | +0.0014 | False |
| sma50_1h | +0.0007 | False |
| sma200_1h | +0.0007 | False |
| sma50_d1 | -0.0002 | False |
| sma200_d1 | +0.0004 | False |
| golden_cross_d1 | +0.0006 | False |
| btc_ret_lag_1 | -0.0021 | False |
| btc_ret_lag_4 | -0.0100 | False |
| btc_ret_lag_12 | -0.0038 | False |
| btc_beta_60 | -0.0003 | False |
| btc_beta_residual_60 | -0.0003 | False |
| btc_residual_ch9 | -0.0000 | False |

## Deflation analysis

Noise scales: `fold` = empirical purged-WF per-fold SE (honest for
overlapping labels); `iid` = Mann-Whitney null SE (contrast — optimistic).

| track | observed | N | noise | SE | exp_max_under_null | deflated_prob |
|---|---|---|---|---|---|---|
| A: base AUC edge | v3 best documented AUC 0.5284 | 17 | fold | 0.00899 | 0.51643 | 0.908 |
| A: base AUC edge | v3 best documented AUC 0.5284 | 17 | iid | 0.00141 | 0.50258 | 1.000 |
| A: base AUC edge | v3 best documented AUC 0.5284 | 100 | fold | 0.00899 | 0.52275 | 0.735 |
| A: base AUC edge | v3 best documented AUC 0.5284 | 100 | iid | 0.00141 | 0.50357 | 1.000 |
| A: base AUC edge | v3 best documented AUC 0.5284 | 200 | fold | 0.00899 | 0.52486 | 0.653 |
| A: base AUC edge | v3 best documented AUC 0.5284 | 200 | iid | 0.00141 | 0.50390 | 1.000 |
| A: base AUC edge | v3 scorecard OOF AUC 0.5120 | 17 | fold | 0.00899 | 0.51643 | 0.311 |
| A: base AUC edge | v3 scorecard OOF AUC 0.5120 | 17 | iid | 0.00141 | 0.50258 | 1.000 |
| A: base AUC edge | v3 scorecard OOF AUC 0.5120 | 100 | fold | 0.00899 | 0.52275 | 0.116 |
| A: base AUC edge | v3 scorecard OOF AUC 0.5120 | 100 | iid | 0.00141 | 0.50357 | 1.000 |
| A: base AUC edge | v3 scorecard OOF AUC 0.5120 | 200 | fold | 0.00899 | 0.52486 | 0.076 |
| A: base AUC edge | v3 scorecard OOF AUC 0.5120 | 200 | iid | 0.00141 | 0.50390 | 1.000 |
| B: best channel lift | RSI-rank Delta 0.0124 | 17 | fold | 0.00899 | 0.01643 | 0.327 |
| B: best channel lift | RSI-rank Delta 0.0124 | 17 | iid | 0.00141 | 0.00258 | 1.000 |
| B: best channel lift | RSI-rank Delta 0.0124 | 100 | fold | 0.00899 | 0.02275 | 0.125 |
| B: best channel lift | RSI-rank Delta 0.0124 | 100 | iid | 0.00141 | 0.00357 | 1.000 |
| B: best channel lift | RSI-rank Delta 0.0124 | 200 | fold | 0.00899 | 0.02486 | 0.083 |
| B: best channel lift | RSI-rank Delta 0.0124 | 200 | iid | 0.00141 | 0.00390 | 1.000 |

## Findings, plain English

### Track A.1 — v3's best documented AUC (0.5284): MARGINAL

This is the best result the entire feature search ever produced — top-40
features by gain, tuned hyperparameters. Under the honest fold-level SE:

| N (search size) | exp_max under null | deflated_prob | Reading |
|---|---|---|---|
| 17 | 0.51643 | **0.908** | Almost clears the 0.95 bar. Real, but borderline. |
| 100 (headline) | 0.52275 | **0.735** | **Marginal.** Above the floor, short of confidence. |
| 200 | 0.52486 | **0.653** | Still above the floor — but weak. |

The project's best cherry-picked, tuned configuration is *probably* real
signal. But once you account for the ~100 trials behind it (channel
probes + 81-config hyperparameter grid + horizon sweep), it doesn't clear
the 0.95 confidence bar that would justify "deploy with conviction."

iid column for the same observed value: 1.000 across all N. Naive
significance testing says "14σ above chance, ship it." Wrong, for the
non-IID reasons in the noise-scales section above.

### Track A.2 — v3's honest deployable AUC (0.5120): BELOW the noise floor

The scorecard's purged-WF OOF AUC — what v3 actually delivers when measured
properly, not cherry-picked.

| N | exp_max under null | deflated_prob | Reading |
|---|---|---|---|
| 17 | 0.51643 | **0.311** | Below the floor. |
| 100 | 0.52275 | **0.116** | Well below. |
| 200 | 0.52486 | **0.076** | Indistinguishable from a noisy random search. |

The gap between 0.5284 (marginal) and 0.5120 (noise) is **0.0164 of AUC that
is selection + tuning artifact**, not real predictive signal. When you remove
the cherry-picking, what's left looks like search luck.

### Track B — "the only confirmed PASS" (RSI-rank +0.0124): BELOW the noise floor

RSI-rank survivorship-aware Δ = +0.0124 was logged as the single positive
finding from 16 channel candidates. Against null Δ = 0, fold SE:

| N | exp_max under null | observed Δ | deflated_prob | Reading |
|---|---|---|---|---|
| 17 | **+0.01643** | +0.0124 | **0.327** | Below the floor. |
| 100 | +0.02275 | +0.0124 | **0.125** | Well below. |
| 200 | +0.02486 | +0.0124 | **0.083** | Basically noise. |

If you ran 17 channel-add probes on a *true-null* model (no real signal
anywhere), the *best* one you'd find by chance would average **+0.0164**.
RSI-rank's "win" is **+0.0124 — below that floor**. The "one confirmed
PASS" is almost certainly selection noise.

### Why the iid column is 1.000 everywhere

Every iid row pegs at 1.000 — including the v3 OOF AUC of 0.5120 (which is
actually noise) and RSI-rank's +0.0124 (also actually noise).

The reason iid is so badly wrong: 167k labels with 4h-horizon triple-barrier
windows on 1h bars are not independent draws. The textbook AUC variance
under-counts the noise by ~6×. Every "the new channel improved AUC by
+0.003 with p < 0.0001" claim made on overlapping financial labels is
*systematically over-confident*. This isn't a quirk of one calculation —
it's the textbook example of why financial ML needs special treatment.

## What this changes for the roadmap

Three things are simultaneously true:

1. **There IS a small real edge in v3.** The headline 0.735 isn't zero — at
   N=17 fold the cherry-picked best (0.5284) hits 0.908, close to clearing
   the bar. *Something* learned from the data.
2. **The honest model is noise.** The actual deployed OOF AUC (0.5120)
   deflates to 0.116 at N=100 fold. Once you remove cherry-picking, what's
   left looks like search luck.
3. **The "confirmed wins" probably weren't.** RSI-rank's +0.0124 lift is
   below the best-of-N noise floor. The 16-failed-1-passed narrative is
   plausibly "16 probes failed, 1 looked good by chance."

**Implications:**

- The scorecard's "1 of 4 gates" verdict (Session 58.71l) gets sharper. v3
  isn't just "not deployable after fees" — the underlying ranking signal
  isn't even statistically separable from search noise *before* fees enter
  the picture.
- **The case for more single-add channel probes on this cache is dead.**
  That gate cannot be cleared honestly. The best you could reasonably
  expect is another result that *looks* good but doesn't survive deflation.
- **Structural-change paths win by comparison.** The bar-structure
  exploration (SP1/SP2, shipped — Sessions 58.71n–o) is exactly this kind
  of pivot — change the data, not just the features. **#20 CUSUM event
  filters** and **#23 regime-conditional boosters** are similarly
  structural and remain defensible.
- **The cheap hygiene moves are essentially mandatory.** **#17 CPCV** and
  **#18 sample-uniqueness weighting** aren't "polish on a working system" —
  they're "stop overstating the signal you have." Especially #18 — the
  non-IID correction the iid-vs-fold gap quantified.
- **#22 DoubleEnsemble and #24 GMADL are less attractive.** Both layer
  machinery on a primary whose signal is fragile; they'd inherit the
  fragility.

**Honest read for what to do next.** Don't pour weeks into another single-add
channel probe on this cache. Either go structural — and wait for the SP1/SP2
operator-run verdict to tell you if dollar bars beat time bars — or accept
that v3 is "slightly better than coin-flip" and ship/decommission it on
that framing.

## Glossary

Alphabetical. Where a term is explained in depth in the main sections above, the glossary entry is the one-line summary.

**AFML** — Marcos López de Prado, *Advances in Financial Machine Learning* (2018). The methodological source for purged CV, the triple-barrier label, and the Deflated Sharpe Ratio used in this analysis.

**AUC** — Area Under the (Receiver Operating Characteristic) Curve. Single-number ranking-quality metric for a binary classifier; 0.5 = random, 1.0 = perfect; the probability that a random positive scores higher than a random negative. See the "What AUC is" section above.

**Bailey & López de Prado** — Authors of the Deflated Sharpe Ratio (2014) and the expected-max-of-N-normals approximation `(1−γ)·Φ⁻¹(1−1/N) + γ·Φ⁻¹(1−1/(N·e))` used here, where γ ≈ 0.5772 is the Euler-Mascheroni constant.

**Deflated probability** — `Φ((observed − expected_max_under_null) / SE)`. The probability the observed edge exceeds the best-of-N selection-noise floor. ≥ 0.95 = real edge; ~ 0.5 = on the floor; < 0.5 = below the floor (probably noise).

**Embargo** — A small gap (4h here) added after each validation region in purged-WF CV. Drops training samples whose temporal proximity to the validation period would let serial correlation leak the answer. AFML technique.

**Expected max under null** — `center + SE · E[max of N standard normals]`. The result a true-null search of N trials would produce on average for its *best* trial — i.e. the "noise floor" the observed result must beat to count as real.

**Fold-level SE** — Empirical standard deviation of an AUC across the 5 purged-WF CV folds: `[0.516, 0.507, 0.527, 0.523, 0.529]` → SE 0.00899. The honest noise scale for overlapping financial labels. See "The two noise scales" section.

**H0 (null hypothesis)** — In this doc, "the model has no real predictive signal." Track A null = AUC 0.5; Track B null = Δ 0.

**iid** — Independent and identically distributed. Standard statistics assumption that data points carry no information about each other. Violated heavily here by overlapping triple-barrier labels — see "The two noise scales" section.

**Mann-Whitney null SE** — Textbook closed-form AUC variance under H0: `sqrt((n_pos+n_neg+1)/(12·n_pos·n_neg))`. Treats samples as iid; here = 0.00141, which under-counts the real noise by ~6× because cache samples aren't iid.

**N (trial count)** — How many configurations were tried before the best one was selected. Higher N = harder for any single result to beat the noise floor. Tiered here as **17** (channel candidates only), **100** (full search incl. 81-config hyperparameter grid + 5 horizons), **200** (conservative).

**OOF (out-of-fold)** — For each sample, the prediction from a CV model that did NOT see that sample during training. Aggregating all OOF predictions gives one "honest" prediction per sample.

**Purged** — AFML CV modification: drop training samples whose forward-label window overlaps the validation fold. Prevents the model from peeking at validation-period information through its training labels.

**Purged-WF OOF AUC** — AUC computed on out-of-fold predictions from a 5-fold purged walk-forward CV with embargo. The most rigorous AUC available on this data — time-ordered, purged, OOF. v3's purged-WF OOF AUC = 0.5120.

**RSI-rank** — A candidate channel from the probe history: each product's percentile rank, among the top-20, of its RSI(14) at time t. The single "confirmed PASS" probe (+0.0124 survivorship-aware) — which this diagnostic shows is plausibly selection noise.

**Scorecard** — The deployment-aligned XGB evaluation built in Session 58.71l (precision-at-gate, expected return after fees, paper-Sharpe, ECE; 4 hard gates). Companion to this diagnostic — the scorecard answers "is v3 deployable economically?" (no, 1/4 gates); this doc answers "is v3's underlying signal even real?" (marginal).

**Selection bias** — The inflation of "best" results when many configurations are tried. Even a true-null search will produce a best result that beats the per-trial null by `SE · E[max of N normals]`. This entire diagnostic exists to correct for it.

**Survivorship-aware top-20** — The 20-product universe used in v3's training pool. Selected per `tools/pid_snapshot.survivorship_aware_top_n` to avoid the bias of recently-listed winners crowding out historically-tracked products.

**Track A** — Deflation of v3's base AUC edge against AUC = 0.5. Two observed values: v3 best documented (0.5284, cherry-picked best of the hyperparameter search) and v3 scorecard OOF (0.5120, the honest deployable).

**Track B** — Deflation of the best channel-add lift against Δ = 0. Observed: RSI-rank +0.0124.

**Triple-barrier** — Label scheme from AFML: starting at sample bar t, the label is set by whichever happens first within the forward window — upper price barrier (UP, +X%), lower barrier (DOWN, −X%), or vertical timeout (sign of `close[t+k] − close[t]`). v3 uses ±1% barriers with a 4-bar timeout.

**v3** — The XGB model currently deployed as live driver (`MODEL_BACKEND=xgb`). 350 features (top-40-by-gain at deployment), ±1% triple-barrier label at 4h horizon on 1h candles, fit on the survivorship-aware top-20 product pool.

**Walk-forward (WF) CV** — Cross-validation that respects time order: each fold trains on earlier data, validates on later data. The base scheme that "purged-WF" modifies with the purge + embargo steps.

## See also

- `2026-05-22-probe-selection-bias-design.md` — design spec for this analysis
- `xgb_probe_results_log.md` (memory) — verbatim probe history this transcribes
- `xgb_feature_optimization_findings.md` (memory) — AUC-ceiling summary
- `2026-05-18-xgb-scorecard-baseline-results.md` — the v3 deployment verdict (1 of 4 gates)
- `xgb_post_scorecard_roadmap.md` (memory) — the 8-path roadmap that #16 gates
