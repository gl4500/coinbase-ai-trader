"""Retired research probes (Session 58.71h, #311-refactor-g).

Historical single-add probes and one-shot audits that produced findings
documented in CHANGELOG.md / memory but are no longer in active use.
Preserved here as a referenceable record; not imported by production
code or by the active test suite.

If you need to re-run one, restore it to backend/tools/ — note that
inter-probe imports may need the `tools.retired.` prefix.
"""
