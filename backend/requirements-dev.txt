# Development / CI dependencies — not needed in production
-r requirements.txt

# Testing
pytest==8.3.5
pytest-asyncio==0.24.0
pytest-cov==6.0.0
httpx==0.27.2        # already in requirements.txt — version pinned for ASGI testing

# Security scanning
bandit[toml]==1.8.3
pip-audit==2.8.0
detect-secrets==1.5.0

# Linting / formatting
ruff==0.9.0

# Pre-commit
pre-commit==4.0.1
