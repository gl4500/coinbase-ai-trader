fastapi==0.141.1
uvicorn[standard]==0.30.6
aiosqlite==0.20.0
python-dotenv>=1.2.2
httpx==0.27.2
websockets==13.1
pydantic==2.9.2
python-multipart>=0.0.18
PyJWT>=2.13.0
cryptography>=50.0.0
numpy>=1.26.0
pandas>=2.0.0
xgboost>=2.0.0
# Transitive-dependency security pins (Snyk 2026-08-09) — override vulnerable
# versions otherwise pulled in by fastapi / uvicorn / httpx:
starlette>=1.3.1         # fixes ReDoS/SSRF/resource-throttling/name-resolution (Snyk High)
click>=8.3.3             # uvicorn dep; fixes command injection
idna>=3.15               # httpx/fastapi dep; fixes ReDoS
# PyTorch — install separately (already installed):
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==7.1.0
