[project]
name = "coinbase-ai-trader"
version = "1.0.0"
requires-python = ">=3.11"

# ── pytest ────────────────────────────────────────────────────────────────────
[tool.pytest.ini_options]
testpaths        = ["backend/tests"]
asyncio_mode     = "auto"          # pytest-asyncio auto mode
python_files     = ["test_*.py"]
python_classes   = ["Test*"]
python_functions = ["test_*"]
addopts          = [
    "-v",
    "--tb=short",
    "--strict-markers",
    "--color=yes",
]
markers = [
    "slow: marks tests as slow (deselect with -m 'not slow')",
    "integration: tests that hit a real DB or network",
]

# ── bandit (Python SAST) ──────────────────────────────────────────────────────
[tool.bandit]
targets    = ["backend"]
exclude_dirs = ["backend/tests", ".venv"]
skips      = [
    "B101",   # assert statements in tests — expected
]
severity   = "medium"    # flag MEDIUM and above

# ── ruff (linting + formatting) ───────────────────────────────────────────────
[tool.ruff]
line-length = 100
target-version = "py311"
src = ["backend"]
exclude = [".venv", "dist", "node_modules", "frontend"]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "B",   # flake8-bugbear
    "I",   # isort
]
# Security linting (flake8-bandit / "S") is intentionally NOT selected: the
# dedicated Bandit SAST job in .github/workflows/ci.yml is the security gate,
# so enabling ruff's S-rules here would duplicate it.
ignore = [
    "B008",  # do not perform function call in argument defaults (FastAPI Depends)
    "E501",  # line length handled by ruff formatter
]
# E402 is ignored where a sys.path bootstrap must run before imports:
# main.py (launcher .venv shim) and the test modules (BACKEND path insert).
per-file-ignores = { "backend/tests/*" = ["B", "E402"], "backend/main.py" = ["E402"] }

[tool.ruff.lint.isort]
known-first-party = ["config", "database", "agents", "clients", "services"]
